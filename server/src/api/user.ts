//import { Router } from "express";
//import controllers from "../controllers";

export const initUserApi = () => {
    //const router = Router();
    //router.get("/:id", controllers.UserController.show);
    //router.patch("/:id", controllers.UserController.update);
    //router.delete("/:id", controllers.UserController.delete);
    //return router;
};
